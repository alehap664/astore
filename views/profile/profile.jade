extends info

block details

    .row
        h3 Thông tin cá nhân
        .col-sm-6
            .table-responsive
                table.table.table-condensed
                    tbody
                        tr
                            td Tên đầy đủ
                            td= customer.FullName
                        tr
                            td Địa chỉ
                            td= customer.StreetAddress
                        tr
                            td
                            td= customer.PostCode + ' ' + customer.City
                        tr
                            td
                            td= customer.Country
                        tr
                            td Số điện thoại
                            td= customer.Phone
                        tr
                            td Email
                            td= customer.Email
                        tr
                            td Tên người dùng
                            td= customer.Username
    form.dev-form(action='/usr/' + customer.Username + '/edit', method='GET')
        button.btn.btn-default(type='submit')
            i.fa.fa-edit
            = ' Sửa thông tin'

    form.dev-form(action='/usr/' + customer.Username + '/change-password', method='GET')
        button.btn.btn-default(type='submit')
            i.fa.fa-key
            = ' Đổi mật khẩu'
    br
    br
    br