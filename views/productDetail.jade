extends layout

block content
    section
        .container
            .row
                //.col-sm-3
                //    .left-sidebar
                //        h2 Categories
                //        //category-products
                //        #accordian.panel-group.category-products
                //            if categories
                //                each category in categories
                //                    .panel.panel-default
                //                        .panel-heading
                //                            h4.panel-title
                //                                a(href=category.SlugLink)= category.CategoryName
                //        ///category-products

                //.col-sm-9.padding-right
                if product
                    // product-details
                    .product-details
                        //product-image
                        .col-sm-5
                            .view-product
                                img(src='/img/products/' + product.Image, alt=product.ProductName)
                        ///product-image

                        //product-information
                        .col-sm-7
                            .product-information
                                h2= product.ProductName

                                p
                                    b Mô tả:
                                    = ' ' + product.Description

                                - var availability = 'Còn hàng'
                                if product.UnitsInStock < 1
                                    - availability = 'Hết hàng'

                                p
                                    b Trạng thái sản phẩm:
                                    = ' ' + availability
                            

                                span
                                    span= product.ProductPrice  + ' VND'
                                    form(action='/cart/' + product.ProductID + '/add', method='POST')
                                        label Số lượng:
                                            input(type='text', name='quantity', value='1')
                                        br
                                        button.btn.btn-default.add-to-cart(type='submit')
                                            i.fa.fa-shopping-cart
                                            | Thêm vào giỏ

                                a(href='')
                                    img.share.img-responsive(src='images/product-details/share.png', alt='')
                        ///product-information
                    ///product-details